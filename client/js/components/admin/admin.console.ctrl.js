/**
 * Created by zoonman on 11/27/16.
 */

app.controller(
    'AdminConsoleController',
    ['socket',
     '$scope',
     '$routeParams',
     '$timeout',
     'PerfectScrollBar',
     '$localStorage',
     '$document',
     function(socket,
              $scope,
              $routeParams,
              $timeout,
              PerfectScrollBar,
              $localStorage,
         $document) {

       $scope.command = '';
       //


     }
    ]
);
